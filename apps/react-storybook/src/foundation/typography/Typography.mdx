import { Meta } from "@storybook/blocks";
import "./Typography.scss";

export const HEADINGS = [
	"heading-xxl-semibold",
	"heading-xl-semibold",
	"heading-xl-regular",
	"heading-l-semibold",
	"heading-l-regular",
	"heading-m-semibold",
	"heading-m-regular",
	"heading-s-semibold-caps",
	"heading-xs-semibold-caps",
	"heading-xs-regular-caps"
];

export const BODY = [
	"body-m-semibold",
	"body-m-regular",
	"body-s-bold",
	"body-s-semibold",
	"body-s-regular",
	"body-xs-bold",
	"body-xs-semibold",
	"body-xs-regular",
	"body-xs-light"
];

export const LABELS = [
	"label-m-regular"
];

export const NUMERIC = [
	"numeric-m-semibold",
	"numeric-s-semibold"
];

<Meta
	parameters={{
		viewMode: "docs",
		options: { isToolshown: false }
	}}
	title="Foundation/Typography"
/>

# Typography

_Kelvin's_ primary typeface is Proxima Nova. **Proxima Nova** was designed to combine modern proportions with a geometric appearance. It allows you to build interfaces with a look and feel that combines a touch of user-friendly, contemporary, fresh, and timeless design.
We will use the typeface at various weights, primarily _Light [300]_, _Regular [400]_, _Semibold [600]_, and _Bold [700]_.

**Note**: Each variation has a `@mixin` available for use. In order to use those _mixins_, you'll need to import the styles from `@kelvininc/ui-components/assets/styles`.

## Headings

<ul className="typography-container">
	{HEADINGS.map((typosKey) => (
		<li key={typosKey} className="typography">
			<span className={typosKey}>{typosKey}</span>
		</li>
	))}
</ul>

## Body

<ul className="typography-container">
	{BODY.map((typosKey) => (
		<li key={typosKey} className="typography">
			<span className={typosKey}>{typosKey}</span>
		</li>
	))}
</ul>

## Labels

<ul className="typography-container">
	{LABELS.map((typosKey) => (
		<li key={typosKey} className="typography">
			<span className={typosKey}>{typosKey}</span>
		</li>
	))}
</ul>

## Numeric

<ul className="typography-container">
	{NUMERIC.map((typosKey) => (
		<li key={typosKey} className="typography">
			<span className={typosKey}>{typosKey}</span>
		</li>
	))}
</ul>
