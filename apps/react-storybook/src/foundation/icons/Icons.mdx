import { Meta } from "@storybook/blocks";
import "./Icons.scss";
import {
	EIllustrationName,
	EIconName,
	KvIcon,
	KvIllustration
} from "@kelvininc/react-ui-components";

<Meta
	parameters={{
		viewMode: "docs",
		options: { isToolshown: false }
	}}
	title="Foundation/Icons & Illustrations"
/>

# Spatial System

All our icons and illustrations are build with [Scalable Vector Graphics (SVGs)](https://www.w3.org/TR/SVG/intro.html), crafted to a variety of devices, screen sizes, and display resolutions keeping quality scalability and responsiveness.

## Icons pack

<div className="svgs-container">
	{Object.values(EIconName).map((icn) => (
		<div key={icn} className="icon-wrapper">
			<div className="box-wrapper">
				<KvIcon name={icn} custom-class="icon-24" />
			</div>
			<div className="description">
				<span className="code">{icn}</span>
			</div>
		</div>
	))}
</div>

## Illustrations pack

<div className="svgs-container">
	{Object.values(EIllustrationName).map((illustration) => (
		<div key={illustration} className="ilus-wrapper">
			<div className="box-wrapper">
				<KvIllustration
					name={illustration}
					custom-class="illustration-full-size"
				/>
			</div>
			<div className="description">
				<span className="code">{illustration}</span>
			</div>
		</div>
	))}
</div>
